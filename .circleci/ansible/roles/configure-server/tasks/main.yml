---
- name: Update packages
  apt:
    update_cache: yes
  tags:
    - system

- name: Install nvm
  shell: apt install curl  && curl https://raw.githubusercontent.com/creationix/nvm/master/install.sh | bash

- name: install node13.8.0
  shell: nvm install 13.8.0
